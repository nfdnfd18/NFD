import React from 'react';
import { useTranslation } from 'react-i18next';

const UserHomePage = () => {
  const { t } = useTranslation();
  return (
    <div>{t('user.welcome') || 'WELCOME USER TO YOUR ACCOUNT'}</div>
  )
}

export default UserHomePage